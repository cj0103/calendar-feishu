# 修复日历列表授权问题和更新页面标题

## 问题描述

1. **首页右上角图标提示**：仍然显示"飞书日程测试页面"，需要改为"飞书日历管理中心"
2. **点击刷新列表报错**："错误：未登录，请先授权"
   - 原因：`feishu:getCalendarList` 使用 `user_access_token`，但用户没有进行 OAuth 授权
   - 当前应用使用的是 `tenant_access_token`（应用身份）

## 解决方案

### 问题 1：更新页面标题

**文件**: `src/renderer/src/App.tsx`

**修改位置**: 第 627 行

**修改内容**:
```typescript
// 原代码
title="飞书日程测试页面"

// 修改为
title="飞书日历管理中心"
```

### 问题 2：修复授权错误

**方案 A**: 修改 `feishu:getCalendarList` 使用 `tenant_access_token`（推荐）

**文件**: `src/main/index.ts`

**修改位置**: 第 414-436 行

**原因**: 
- 当前应用主要使用 `tenant_access_token` 进行 API 调用
- `feishuCalendarAPI.getCalendarList()` 已经使用 `tenant_access_token`
- 保持一致性，避免用户需要额外授权

**修改内容**:
```typescript
// 使用 tenant_access_token 获取日历列表
ipcMain.handle('feishu:getCalendarList', async () => {
  try {
    // 直接使用 feishuCalendarAPI 的方法（已使用 tenant_access_token）
    const calendars = await feishuCalendarAPI.getCalendarList()
    return { success: true, calendars }
  } catch (error: any) {
    console.error('获取日历列表失败:', error)
    return { 
      success: false, 
      error: error.message || '获取日历列表失败',
      hint: '请检查飞书应用配置和权限设置'
    }
  }
})
```

**方案 B**: 添加友好的错误提示（可选增强）

在 `FeishuTestPage.tsx` 中改进错误显示：

```typescript
const loadCalendars = async () => {
  setIsLoadingCalendars(true)
  setError(null)
  try {
    const result = await window.api.feishu.getCalendarList()
    if (result.success) {
      setCalendars(result.calendars || [])
    } else {
      // 添加更详细的错误提示
      const errorMsg = result.error || '加载日历列表失败'
      const hint = result.hint || ''
      setError(errorMsg + (hint ? `\n${hint}` : ''))
    }
  } catch (err: any) {
    console.error('❌ 加载日历列表失败:', err)
    setError(err.message || '加载日历列表失败')
  } finally {
    setIsLoadingCalendars(false)
  }
}
```

## 实施步骤

1. ✅ 分析问题原因（已完成）
2. 修改 `App.tsx` 中的页面标题
3. 修改 `index.ts` 中的 `feishu:getCalendarList` IPC 处理器
4. （可选）在 `FeishuTestPage.tsx` 中增强错误提示
5. 测试验证：
   - 点击右上角图标，确认提示文字已更新
   - 打开飞书日历管理中心，点击刷新列表，确认能正常加载日历
6. 提交代码

## 预期结果

1. 首页右上角图标提示显示"飞书日历管理中心"
2. 点击刷新列表能正常加载日历列表，不再提示"未登录"
3. 如果飞书应用配置或权限有问题，显示更详细的错误提示

## 注意事项

1. `feishuCalendarAPI.getCalendarList()` 已经使用了 `tenant_access_token`，直接调用即可
2. 确保飞书应用有以下权限：
   - 应用读取自身信息
   - 日历相关权限（读、写）
3. 如果用户是新安装，需要确保已在飞书开放平台正确配置应用并发布
